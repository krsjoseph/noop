source "https://rubygems.org"

# TestFlight / App Store automation. Pinned via Gemfile.lock and run with `bundle exec fastlane …`
# so every machine and CI runner uses the same version. See fastlane/Fastfile for the lanes.
gem "fastlane"
